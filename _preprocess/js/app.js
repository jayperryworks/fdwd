(function($) {
  
  // ================================================================
  // FDWD behaviors
  // ================================================================

  var fdwd = { // namespace setup

    config : {

      // Settings
      // -> global vars go here
      // --------------------------------------------------------------- 

    },

    // Setup
    // ---------------------------------------------------------------
    init : function(config) {
      $.extend(fdwd.config, config);


    },

    // Methods
    // ---------------------------------------------------------------

    
  };

  
  $(window).load(function() {
    fdwd.init();
  });
})(jQuery);
